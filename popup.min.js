document.addEventListener("DOMContentLoaded",(()=>{const folderListElement=document.getElementById("folderList"),loadingElement=document.createElement("div");loadingElement.textContent="Loading...",folderListElement.appendChild(loadingElement),chrome.bookmarks.getTree((bookmarkTreeNodes=>{folderListElement.innerHTML="";const bookmarksBar=bookmarkTreeNodes[0].children[0].children;if(bookmarksBar.forEach(((node,index)=>{if(!node.url){const folderElement=document.createElement("div");folderElement.textContent=`Folder ${index+1}: ${node.title}`,folderElement.className="folder",folderListElement.appendChild(folderElement)}})),0===bookmarksBar.length){const errorMessage=document.createElement("div");errorMessage.textContent="No bookmark folders found.",folderListElement.appendChild(errorMessage)}})),document.getElementById("howToUseBtn").addEventListener("click",(()=>{chrome.tabs.create({url:chrome.runtime.getURL("how-to-use.html")})}))}));